{
  "name": "solidar-bairro-backend",
  "version": "1.0.0",
  "description": "Backend API para Solidar Bairro",
  "main": "src/server.js",
  "scripts": {
    "start": "NODE_ENV=production node src/server.js",
    "dev": "NODE_ENV=development nodemon --inspect src/server.js",
    "start:prod": "pm2 start src/server.js --name solidar-backend",
    "deploy": "firebase deploy",
    "serve": "firebase emulators:start",
    "test": "jest"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "firebase-functions": "^4.4.1",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.6.2"
  }
}